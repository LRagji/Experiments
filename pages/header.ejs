<div id="alertPane" class="alert" style="font-weight:bold; position:fixed;top: 0px; left: 0px; width: 100%;z-index:9999;border-radius:0px;display:none" role="alert">
    A simple success alert with <a href="#" class="alert-link">an example link</a>. Give it a click if you like.
</div>
<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
    <a class="navbar-brand font-weight-bold text-uppercase" href="/"><i class="fas fa-briefcase-medical"></i> HEALTH MALL</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <form class="form-inline my-2 w-100 input-group">
            <input class="form-control" type="search" placeholder="Search" aria-label="Search">
            <div class="input-group-append">
                <button class="btn bg-light" type="submit"><i class="fas fa-search"></i></button>
            </div>
        </form>


        <ul class="navbar-nav mr-2">
            <li class="nav-item active">
                <button onclick="<% if (user) { %><%= 'window.location.href = "/secure/profile"';%> <% } else{ %>
                    <%= 'window.location.href = "/secure/login";' %>
                    <% } %>
                    " class="btn btn-link my-2 mr-0 my-sm-0" type="submit"><i class="fas fa-user-circle"></i>
                    <% if (user) { %>
                    <%- user.first %>
                    <% } else{ %>Login
                    <% } %>
                </button>
            </li>
            <li class="nav-item active">
                <button class="btn btn-link my-2 mr-0 my-sm-0" type="submit"><i class="fas fa-shopping-cart"></i>
                    Cart<span id="shoppingCart" class="badge ml-1 badge-secondary"><%= pageData.cartItems %></span></button>
            </li>
        </ul>
    </div>
</nav>
<div class="bg-dark my-4">
    <!-- This blank space is left intentionally as fixed top nav bar is floating content -->
    <br>
</div>